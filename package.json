{
  "name": "draftoff",
  "version": "0.0.0",
  "private": true,
  "description": "Multiplayer football draft web app (snake draft + simulated tournament)",
  "packageManager": "pnpm@8.15.4",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev": "pnpm -r --parallel dev",
    "build": "pnpm -r build",
    "lint": "pnpm -r lint",
    "typecheck": "pnpm -r typecheck"
  },
  "devDependencies": {
    "typescript": "^5.4.5"
  },
  "dependencies": {
    "next": "^16.2.9"
  }
}
